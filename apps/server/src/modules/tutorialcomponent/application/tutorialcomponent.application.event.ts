export namespace TutorialcomponentApplicationEvent {
  export namespace TutorialcomponentCreated {
    export const key = 'tutorialcomponent.application.tutorialcomponent.created'

    export type Payload = {
      id: string
      userId: string
    }
  }
}
